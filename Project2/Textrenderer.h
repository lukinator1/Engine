#pragma once
#include "Rendering/Rendermanager.h"
#include "Component.h"
#include "Mathlibrary/vector2.h"
class Textrenderer //this class is on hold for now
{
/*public:
	Rendertext textrenderer;
	std::string text;
	vector2 position;
	vector3 color;
	float scale;
	void renderComponent(Transforming t, Shader s) {
		/*textrenderer.renderText(text, position, color, scale);
		textrenderer.renderText("> The quick brown fox jumped over the lazy dog. 1234567890", 0.0f, 300.0f, vector3(0.4, 0.3, 0.8), .7f);
	}
	Textrenderer(std::string txt, vector2 pos, vector3 _color, float _scale) {
		text = txt;
		position = pos;
		color = _color;
		scale = _scale;
	}
	Textrenderer() {
		text = "";
		position = vector2(0.0f,0.0f);
		color = vector3(1.0, 1.0, 1.0);
		scale = 1.0f;
	}
	~Textrenderer();*/
};

